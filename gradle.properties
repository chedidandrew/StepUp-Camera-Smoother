org.gradle.jvmargs=-Xmx2G -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=false

minecraft_version=26.3
loader_version=0.19.5
minimum_loader_version=0.19.5
loom_version=1.17.21
fabric_api_version=0.160.7+26.3
modmenu_version=21.0.0-beta.1
junit_version=5.14.2

mod_version=0.2.0
maven_group=dev.chedidandrew
archives_base_name=smart-stepup-camera-smoother

# Optional local and CI compatibility profiles. All version IDs below are exact,
# immutable Modrinth versions for Minecraft 26.3 Fabric (except unavailable StepItUp).
include_stepitup_runtime=false
include_modmenu_runtime=false
stepitup_version_id=
cloth_config_version_id=eZ3xIIuk
modmenu_version_id=lcxkZSq6
text_placeholder_version_id=lXytLqWj

# Release publication must fail closed until the candidate jar is verified.
# The final gate-only commit sets release_ready=true and records that exact jar
# digest without changing inputs to the playable jar.
release_ready=false
release_jar_sha256=
